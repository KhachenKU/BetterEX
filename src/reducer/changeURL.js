

const changeTXColorReducer = (state = '', action) => {
    switch (action.type) {
        case 'CHANGEURL':
            return 
        default:
            return state
    }
}

export default changeTXColorReducer;